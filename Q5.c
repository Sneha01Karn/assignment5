#include<stdio.h>
#include<conio.h>
int main()
{
    float l,w,A;
    printf("Enter the length:");
    scanf("%f",&l);
    printf("Enter the width:");
    scanf("%f",&w);
    A=l*w;
    printf("area of the rectangle is %f",A);
    getch();
}
